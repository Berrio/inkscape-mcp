# F05 - Exportaciones

## F05-WP01 (completado el 2026-08-26)

- `exportSpecSchema` implementa el contrato discriminado por formato para PNG,
  PDF, SVG/plain SVG/SVGZ y formatos adicionales gateados. Rechaza propiedades
  cruzadas, áreas custom fuera de PNG, selección/páginas múltiples sin destino
  de directorio, templates inseguros, rutas absolutas/traversal y overwrite sin
  la revisión de salida esperada.
- `ExportAreaNormalizer` se amplió para page, drawing, selección única y área
  custom. La planificación de múltiples páginas o selección `each` queda
  explícita para el planificador de variantes de F05-WP05: nunca se convierte
  en un argv ambiguo.
- Los builders puros generan únicamente flags allowlisted desde el spec ya
  validado. El pipeline común emite etapas ordenadas
  `validated→staging→rendering→verifying→publishing→completed`, propaga
  cancelación y garantiza cleanup del staging del adaptador.
- El manifiesto común registra request normalizado, versión Inkscape, hashes de
  artefactos, estrategia y warnings sin rutas absolutas. `ArtifactStore` admite
  metadata de formato/verificación y publicación de lote opaca con rollback
  de copias si falla un miembro; no se describe como atómica ante crash.
- El dispatcher de verificación solo acepta PNG/PDF/SVG/plain-SVG cuando puede
  probar su estructura. Los demás formatos devuelven falta de verifier hasta
  tener capability, fixture y adaptador real.

### Mini-gate F05-WP01

- `npm run check` pasó: formato, lint, tipos y 105 pruebas (una omitida).
- `npm run test:mcp` pasó con Inkscape local y los transportes moderno/legado.
- Pruebas unitarias cubren la unión discriminada, combinaciones cruzadas,
  rutas/overwrite inseguros, múltiples variantes, builders, progreso/cancelación,
  cleanup, manifiesto y rollback/metadata de artifacts.

## F05-WP03 (completado el 2026-08-26)

- `export_pdf` usa `pdf-lib` 1.17.1 (MIT) para rechazar PDFs no parseables y
  devolver versión, número de páginas, MediaBox, CropBox, bytes y SHA-256.
  La prueba unitaria crea un PDF de dos páginas con boxes conocidos y el flujo
  MCP verifica un PDF real de Inkscape.
- En Inkscape 1.4.4, la exportación completa omite `--export-page` y conserva
  todas las páginas en un solo PDF (`full_document`). Un subset genera una
  copia SVG inmutable podada por IDs de página, la exporta como documento y
  comprueba el conteo solicitado (`prune_subset`).
- `latex=true` está capability-gated y exige el sidecar generado por Inkscape.
  El PDF y `output.pdf_tex` se publican con `AtomicFileStore.commitBatch` bajo
  los mismos locks y revisión del origen. Si una publicación manejada falla,
  se restauran los miembros ya renombrados; esa garantía no se presenta como
  atomicidad ante un crash del proceso. La respuesta expone solo la ruta
  relativa y revisión del sidecar, con `LATEX_SIDECAR_EMITTED`.
- `export_pdf_pages` cubre páginas separadas. Recibe solo IDs de página y un
  directorio relativo existente; cada salida se llama `page-NNN.pdf` según la
  posición original de la página, no el orden de la petición. Genera y verifica
  todos los PDFs antes de publicarlos mediante el commit lógico del grupo.
- El margen PDF no depende de `--export-margin`: en 1.4.4 se expande una copia
  temporal del viewport o de las páginas explícitas y se valida la caja
  producida. El flujo MCP medido para una página A4 ampliada 5 mm por lado
  devuelve 624 × 871 pt; la respuesta declara
  `PDF_MARGIN_EXPANDED_TEMPORARY`.
- La integración MCP usa dos páginas de anchos distintos para confirmar el
  orden del PDF completo y de un subset solicitado en orden inverso. También
  exporta `pdf-nonzero-viewbox.svg` y verifica MediaBox/CropBox 284 × 142 pt
  con la tolerancia de ADR-011; esta build no reproduce el problema 6323.
- ADR-011 registra la sonda de Inkscape 1.4.4: una exportación sin
  `--export-page` conserva un PDF multipágina, mientras que `--export-page`
  crea archivos `_pN.pdf` separados. El subset será poda de una copia SVG; la
  tolerancia de boxes es 0.6 pt y el fixture de viewBox no cero no activa
  workaround en esta build.
- El verificador PDF ahora publica CropBox, bytes y SHA-256. Version, texto a
  paths e ignorar filtros se capability-gatean; `filterDpi` se rechaza antes de
  ejecutar porque la ayuda de esta instalación no anuncia su flag.

### Evidencia F05-WP03 (2026-08-26)

- Archivos: `src/server/index.ts`, `src/storage/revisions.ts`,
  `src/storage/index.ts`, `src/documents/pdf-export.ts`,
  `src/documents/index.ts`, `scripts/test-mcp.mjs`, fixtures PDF y pruebas
  unitarias de storage/documentos.
- `npm run check` pasó: formato, lint, TypeScript y 113 pruebas activas (una
  omitida).
- `npm run test:mcp` pasó con Inkscape 1.4.4 local y los transportes stdio
  moderno/legado; incluye la lectura real de `a4-latex.pdf_tex` y el PDF
  multipágina/subset, además de dos PDFs de página separados solicitados en
  orden inverso y publicados como `page-001.pdf`/`page-002.pdf`, y el fixture
  `viewBox` de origen no cero, además del margen temporal A4 de 5 mm por lado.

## F05-WP04 (completo, 2026-08-29)

### Cierre de F05-T22 (2026-08-29)

- El exportador selection-only usa el resolvedor CSS ya auditado para materializar en estilos inline la cascada soportada, incluidas propiedades heredadas como fill, stroke y font-family. En ese caso no conserva reglas CSS ajenas a la selección y declara `SELECTION_CSS_FLATTENED_SUPPORTED`.
- Si el documento utiliza combinadores, at-rules o variables que salen del subconjunto, conserva el stylesheet y los ancestros necesarios, devuelve `SELECTION_STYLESHEET_PRESERVED_PARTIAL` y nunca lo presenta como cálculo exacto. Los ciclos y referencias rotas siguen fallando antes de publicar.
- El cierre de `defs` se calcula también desde los estilos materializados; imágenes, CSS y fuentes locales que permanezcan referenciados continúan publicándose como assets con URI reescrita y commit conjunto.
- Evidencia: `tests/unit/selection-export.test.ts` cubre herencia CSS, specificity soportada, gradient transitivo y eliminación de defs/reglas no usados. `npm run check` pasó con 247 pruebas totales (1 omitida). `npm run test:mcp` pasó con Inkscape local y verifica una exportación SVG real con `SELECTION_CSS_FLATTENED_SUPPORTED`, stylesheet eliminado, gradiente conservado y font-family materializado.
- T21–T25 están cerradas. El siguiente WP lógico es `F05-WP05` (batch/publicación/crash), que no se ejecuta en este corte.

- `export_svg` ahora construye argv solo con `ExportSpec` validado y
  `buildExportArgv`; ya no ensambla argumentos de proceso en el handler MCP.
- `verifySvg` rechaza cualquier SVG exportado que requiera saneamiento y
  verifica un `viewBox` finito/positivo. Devuelve bytes, hash y `viewBox` al
  resultado MCP.
- La integración real exporta `svg-viewbox-512.svg` como SVG plano y confirma
  que `0 0 512 512` se conserva. Inkscape 1.4.4 no reproduce el problema 6317,
  por lo que no se aplica una corrección temporal.
- `export_svg` acepta opcionalmente `selectionIds` y construye antes de invocar
  Inkscape un SVG autónomo: conserva el contexto de ancestros (por ejemplo,
  transformaciones), incorpora el cierre transitivo local de `defs`/`href`,
  elimina objetos que no pertenecen a la selección y vuelve a normalizar IDs.
  Rechaza referencias rotas o cíclicas. Conserva las hojas `<style>` saneadas y
  cierra sus `url(#id)` locales; devuelve
  `SELECTION_STYLESHEET_PRESERVED_PARTIAL` porque CSS calculado con selectores
  dependientes de hermanos todavía pertenece a F05-T22 y no se anuncia como
  fidelidad exacta.
- Si la selección conservada necesita imágenes, fuentes u otros recursos
  locales, `export_svg` detecta solo los que Inkscape dejó referenciados,
  los publica junto con el SVG bajo `salida.svg.assets/` y reescribe sus URIs.
  SVG y recursos se publican como un lote con rollback de fallos manejados; la
  respuesta expone sus rutas relativas y revisiones, nunca rutas del staging.
- `verifySvg` ahora exige namespace SVG explícito, `viewBox` positivo, IDs
  públicos únicos y cierre de referencias `href`, ARIA y `url(#id)` en
  atributos y CSS. La política preserve-local sigue rechazando recursos
  externos antes de la publicación.
- SVGZ permanece correctamente gateado: la ayuda de Inkscape 1.4.4 local no
  lo lista entre los tipos exportables, por lo que no se expone una operación
  que no pueda verificarse en esta instalación.
- `document_export` inicia la capa unificada para una variante: recibe el
  `ExportSpec` discriminado completo, pero en este corte habilita PNG, PDF
  base, SVG y plain SVG de un solo archivo. Resuelve únicamente rutas/revisiones del
  workspace, normaliza el área, crea un bundle nativo inmutable, verifica el
  artefacto y publica un `Artifact` MCP. PDF especializado, márgenes, opciones
  PNG avanzadas y variantes múltiples se rechazan explícitamente con la tool
  especializada indicada; no se degradan silenciosamente. El PDF base conserva
  texto, filtros y área de documento/dibujo/selección únicos; versión, margen,
  LaTeX, DPI de filtros y subsets continúan en `export_pdf`.
- `document_export_batch` acepta de una a cincuenta variantes base PNG/PDF/SVG
  de la misma revisión y bloquea colisiones de destino antes de renderizar. En
  `all_or_nothing` renderiza y verifica todas las variantes en scratch antes de
  publicarlas mediante `commitBatch`; en `best_effort` devuelve los fallos de
  renderizado y publica únicamente las variantes verificadas. Las opciones
  avanzadas siguen rechazándose de forma explícita hasta reutilizar sus
  adaptadores especializados.
- El mismo tool acepta alternativamente un preset determinista: `print-a4-pdf`,
  `web-png`, `plain-svg` o `icon-pack`. Los presets se expanden a `ExportSpec`
  convencionales (por tanto conservan el planificador y sus verificaciones);
  `icon-pack` produce 16, 24, 32, 48, 64, 128, 256 y 512 px. Su directorio
  relativo se crea segmento por segmento y se canoniza dentro del workspace.
- Cada lote terminado devuelve además un manifiesto portable: modo, revisión y
  ruta relativa de entrada, duración, versión exacta reportada por Inkscape,
  estrategia de publicación, variantes con revisión y fallos. `all_or_nothing`
  declara `file_commit_batch`; `best_effort`, `file_commit_each`. Aún no se
  presenta como commit atómico ante crash ni como marker persistente.

### Evidencia parcial F05-WP04 (2026-08-26)

- `npm run check` pasó con 120 pruebas activas y una omitida.
- `npm run test:mcp` pasó con Inkscape 1.4.4 y transportes stdio moderno/legado.
- La prueba MCP exporta `demo_rect` de `a4.svg` mediante `selectionIds`, abre y
  reexporta el SVG derivado en Inkscape y confirma el warning
  `SELECTION_EXTRACTED_AUTONOMOUSLY`. Además, crea una selección con clase CSS
  y gradiente de `defs`; la exportación real conserva su regla, el gradiente y
  declara su fidelidad parcial. Un PNG local se exporta como selección y la
  prueba verifica su copia publicada, su URI reescrita y sus bytes exactos.
  Las exportaciones reales de ambos transportes pasan también el nuevo cierre
  estricto de IDs y referencias internas.
- La prueba MCP llama `document_export` con un `ExportSpec` PNG transparente y
  otro PDF base, y comprueba en ambos la salida relativa, revisión y URI opaca
  del artefacto publicado.
- En una selección SVG con regla contextual `#card .selected`, el exportador
  conserva el ID único de `card` junto con el hijo seleccionado. La prueba MCP
  reabre el SVG plano derivado mediante Inkscape y confirma esa clausura de
  contexto; ancestros repetidos y CSS fuera del subconjunto siguen marcados
  como fidelidad parcial.
- La suite MCP rasteriza ese SVG original y su selección autónoma a 80 px con
  Inkscape, decodifica PNG no interlazado de 8 bits y compara RGBA con
  tolerancia de un nivel por canal. El fixture actual produce cero píxeles
  distintos; no sustituye todavía el corpus visual de PDF/PNG/SVG completo.
- La misma prueba usa `document_export_batch` en modo `all_or_nothing` para
  PNG de página y dibujo, y comprueba dos éxitos y ningún fallo con Inkscape.
- Esa misma ejecución comprueba que el manifiesto declara `file_commit_batch`,
  contiene las dos variantes y conserva una versión no vacía de Inkscape.
- La prueba MCP además ejecuta `web-png` sin crear previamente su directorio,
  y confirma la publicación de `preset-web/web-1200.png`; las cuatro
  expansiones y los nombres deterministas del icon-pack se cubren en unidad.
- Esa prueba también fuerza una segunda variante con una selección inexistente:
  pese a que la primera se renderiza, `all_or_nothing` retorna error y no
  publica `must-not-publish.png`. Esto cubre el rollback de fallos manejados;
  siguen pendientes crash simulation, commit marker/directorio y limpieza de
  staging al reiniciar.
- Con el mismo SVG, una ejecución `best_effort` fuerza primero esa selección
  inexistente y exporta después un PNG de dibujo. La respuesta conserva el
  fallo en índice 0, publica sólo el índice 1 con firma PNG válida y declara
  `file_commit_each` en su manifiesto.
- `document_export_batch` acepta un `timeoutMs` acotado por la configuración
  del runner y lo propaga a cada render nativo. La integración MCP fuerza un
  milisegundo y confirma que el lote falla sin publicar su PNG. El protocolo de
  cancelación pública se ofrece mediante `delivery=job`, `job_get` y
  `job_cancel`, con ownership por workspace y `AbortSignal` hasta Inkscape. La
  misma integración cancela un PNG A4 de 300 dpi, espera `cancelled` y confirma
  que no existe su archivo final. La comprobación de procesos huérfanos y la
  recuperación persistente tras reinicio siguen abiertas.
- También crea un A4 independiente y comprueba el resultado normativo de
  300 dpi: IHDR de 2480 × 3508 px.
- Una salida con espacios, Unicode, `;` y `&` conserva su nombre y su firma
  PNG válida al atravesar el protocolo MCP y la invocación nativa.

## F05-WP05 (completo, 2026-08-29)

- Objetivo verificable: hacer que un lote `all_or_nothing` sólo se considere publicado cuando exista un receipt final, sin confundir rollback manejado con atomicidad frente a un crash de proceso.
- Prerrequisitos/puertas: F05-WP01–04; `AtomicFileStore.commitBatch`, staging propio, locks/revisiones y el manifest portable ya existen.
- Interfaces disponibles: `document_export_batch`, `createExportBatchManifest`, `AtomicFileStore.commitBatch`, `ScratchManager` y schemas de export existentes.
- No-objetivos: prometer atomicidad filesystem multiarchivo frente a pérdida de energía o borrar automáticamente archivos sin marker cuya procedencia no pueda demostrarse.
- Resultado: `all_or_nothing` prepara todos los renders, calcula sus revisiones, y publica los outputs más el receipt JSON como el último miembro del mismo commit batch. El receipt vive en `.inkscape-mcp-commits/batch-<uuid>.json`, no contiene paths absolutos y declara `publication: manifest_commit`; la respuesta MCP devuelve su ruta relativa. `best_effort` conserva `file_commit_each` porque sus fallos por variante son parte de su contrato.
- Rollback/limpieza: si falla staging o el movimiento del marker, `commitBatch` restaura o elimina los miembros ya publicados; `ScratchManager.withDirectory` limpia rendering/staging en `finally`. Tras un crash fuera de control del proceso pueden quedar archivos físicos sin receipt; no se devuelven como éxito MCP y se documentan como no publicados, en vez de realizar una eliminación insegura.
- Evidencia: `tests/unit/storage.test.ts` inyecta una falla en el tercer movimiento (marker) y confirma que ambos outputs y el marker desaparecen. `tests/unit/export-batch-manifest.test.ts` fija el schema portable. `npm run check` pasó con 248 pruebas totales (1 omitida); `npm run test:mcp` pasó con Inkscape local y verifica que el receipt existe, declara `manifest_commit` y contiene sus outputs.
- T26–T29 están cerradas. El siguiente WP lógico es `F06-WP01`; no se ejecuta en este corte.

## F05-WP02 (completado el 2026-08-26)

- `export_png` cubre áreas page, drawing, selection y custom; la selección se
  comprueba contra el SVG antes de crear el proceso nativo. DPI es mutuamente
  excluyente con píxeles; una dimensión conserva ratio y las dos requieren
  `allowDistortion=true`.
- El fondo admite `document|transparent|solid`: document obtiene únicamente
  `pagecolor`/`pageopacity` y no el desk. Opacidad, snap y todas las opciones
  PNG se convierten desde tipos acotados a flags allowlisted.
- Color mode (Gray/RGB/GrayAlpha/RGBA de 1 a 16 bits), dithering, compresión,
  antialias y snap se consultan en `CapabilityService` antes de la ejecución.
  Una capability ausente responde con un error recuperable y nunca con un flag
  especulativo.
- `verifyPng` entrega firma/IHDR validado, dimensiones, bit depth, color type,
  pHYs/DPI si existe, bytes y SHA-256. La salida MCP declara esta metadata en
  lugar de inferirla desde la solicitud.

### Mini-gate F05-WP02

- `npm run check` pasó: formato, lint, tipos y 106 pruebas activas (una
  omitida).
- `npm run test:mcp` pasó en los transportes moderno y legado con Inkscape
  local. Incluye page/drawing/selection/custom, DPI, fondo sólido, PNG 8-bit y
  PNG transparente RGBA_16 con todos los controles avanzados y un output con
  ruta Unicode.

## Puerta F05-G02 (completada el 2026-08-30)

- `npm run test:f05-g02` inicia el servidor por stdio y exporta un SVG de
  10 × 10 px con un rectángulo rojo tanto en modo `transparent` como en modo
  `solid` con `#123456` y opacidad 0,5.
- Ambos PNG se decodifican: las dimensiones son exactamente 10 × 10, la
  esquina transparente tiene alfa cero, la esquina sólida fija RGB `#123456`
  y alfa 128 (tolerancia de un nivel), y el foreground permanece rojo opaco.
- El smoke verifica que el SHA-256 del SVG fuente no cambia durante ambas
  exportaciones.
- Verificación: `npm run test:f05-g02` y `npm run check` correctos (75
  archivos de prueba, 299 pruebas correctas y una omitida).

## Puerta F05-G03 (completada el 2026-08-30)

- `npm run test:f05-g03` copia el fixture multipágina 1.4.4 al workspace,
  solicita `export_pdf` completo por stdio y verifica la firma PDF publicada.
- La respuesta MCP y el PDF reabierto con `pdf-lib` coinciden en dos páginas:
  la primera de 284 × 142 pt y la segunda de 142 × 142 pt para MediaBox;
  también se fijan ambos CropBox con tolerancia absoluta de 0,6 pt.
- La exportación declara la estrategia `full_document` y el SHA-256 del SVG
  fuente no cambia.
- Verificación: `npm run test:f05-g03` y `npm run check` correctos (75
  archivos de prueba, 299 pruebas correctas y una omitida).

## Puerta F05-G04 (completada el 2026-08-30)

- `npm run test:f05-g04` inicia el servidor por stdio, exporta el mismo SVG
  local en sabor `inkscape` y `plain`, y confirma las revisiones y el `viewBox`
  publicados por cada contrato.
- Ambos derivados se validan como XML SVG: raíz en namespace SVG, `viewBox`
  `0 0 100 60`, rectángulo y círculo esperados. El SVG Inkscape conserva la
  capa editable; el plano no retiene atributos con prefijo `inkscape:`.
- Inkscape headless reabre maestro y derivados, renderiza previews de
  200 × 120 px y la comparación RGBA (tolerancia de un canal) fija cero píxeles
  distintos para ambos sabores. El SHA-256 de la fuente se conserva.
- Verificación: `npm run test:f05-g04` y `npm run check` correctos (75
  archivos de prueba, 299 pruebas correctas y una omitida).

## Puerta F05-G05 (completada el 2026-08-30)

- `npm run test:f05-g05` ejecuta los tests focalizados de storage/recovery y
  después inicia el servidor nuevo por stdio con un scratch root dedicado. La
  inyección de interrupción en el tercer rename (el receipt final) confirma
  que `commitBatch` revierte los dos outputs y el marker; no se interpreta esa
  garantía manejada como atomicidad ante pérdida de energía.
- Antes del arranque el smoke deja un directorio `inkscape-mcp-staging-*`
  obsoleto. El entrypoint lo elimina al reiniciar y, tras el lote, no quedan
  directorios staging propios; sólo se limpia trabajo identificable del
  servidor.
- El lote real `all_or_nothing` publica un PNG y un SVG plano, crea su receipt
  bajo `.inkscape-mcp-commits/batch-*.json`, declara `manifest_commit`, no
  filtra el root absoluto y fija los SHA-256 de ambos miembros. La fuente no
  cambia.
- Tras un corte abrupto externo pueden persistir archivos físicos sin receipt;
  no se eliminan automáticamente porque no se puede demostrar que pertenezcan
  al servidor. Por tanto nunca se anuncian como un lote publicado.
- Verificación: `npm run test:f05-g05` y `npm run check` correctos (75
  archivos de prueba, 299 pruebas correctas y una omitida).
