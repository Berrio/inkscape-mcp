# F07 - Paths y diseno avanzado

## F07-WP01 (cerrado 2026-08-29)

- `parseSvgPathData` es la fuente única de validación de paths creados por el
  MCP. El AST conserva case de comandos, subpaths y comandos relativos; tanto
  los segmentos como sus arrays `values` son mutables.
- `SVG_PATH_LIMITS` fija 100.000 caracteres, 10.000 segmentos y magnitud
  numérica máxima de 1.000.000. La serialización no redondea geometría: usa la
  representación decimal shortest-round-trip y canoniza `-0` a `0`.
- `paths_combine`, `path_break_apart` y `path_reverse` están publicados con
  revisión esperada, IDs tipados y protección de referencias. Las pruebas
  negativas verifican que combine rechaza estilos distintos o un ID referenciado
  y que break-apart no elimina un ID referenciado.
- `paths_boolean` ejecuta exclusivamente las actions nativas allowlisted en un
  input temporal, sanea el resultado y hace commit atómico. Los fixtures MCP
  comparan PNG de union, difference, intersection y exclusion con tolerancia de
  un canal; difference usa `ids[0]` como objetivo e `ids[1]` como cortador.
- La comprobación de actions de Inkscape 1.4.4 confirma `path-division` y
  `path-cut`, ejercitados por MCP. No se anuncian split/fracture porque no están
  disponibles en la lista de actions del baseline.
- Evidencia de cierre: `npm run check` y `npm run test:mcp` pasan con Inkscape
  local; el segundo cubre las cuatro booleanas visuales y division/cut reales.

## F07-WP02 (cerrado 2026-08-29)

- La comprobación del baseline Inkscape 1.4.4 confirmó `path-simplify` y
  `path-flatten`; `path_modify` ejecuta sólo simplify y `paths_flatten` usa la
  acción verificada para 2–100 paths. Inset/outset/dynamic offset no están en
  el baseline: las solicitudes legacy devuelven un error recuperable, sin
  simular una capability ni retirar el contrato ya publicado.
- Cada acción nativa exige IDs públicos distintos, existentes y de tipo path.
  Difference/division/cut exigen además que `ids[0]` quede debajo de `ids[1]`
  en el orden del documento para mantener la semántica direccional.
- Antes de invocar Inkscape, el `NativeInputBundle` aplica una normalización
  interna de IDs inválidos o duplicados y la incluye en el hash inmutable. La
  respuesta devuelve la lista tipada `renamed`; no acepta IDs ni actions libres.
- El cliente MCP real cubre simplify, flatten, rechazo de offset no disponible,
  la normalización de un ID heredado y fixtures con auto-intersección, hueco
  nonzero, arcos, transformaciones y la tolerancia visual ya fijada.
- Evidencia de cierre: `npm run check` y `npm run test:mcp` pasan con Inkscape
  local. La prueba unitaria adicional verifica que un transform interno forma
  parte del hash del bundle y cualquier alteración posterior se detecta.

## F07-WP04 (cerrado 2026-08-29)

- `flowed_text_inspect` limita el inventario y omite IDs que no sean públicos
  seguros. `flowed_text_convert` requiere un único `flowRoot` con una región,
  un rectángulo directo y párrafos de texto plano acotados; rechaza spans,
  regiones múltiples y IDs ambiguos antes de escribir.
- La conversión conserva propiedades tipográficas permitidas, como familia,
  tamaño, espaciado, dirección y writing mode, y siempre devuelve
  `FLOWED_TEXT_LAYOUT_LOST`: no promete wrapping ni equivalencia tipográfica.
- El fixture MCP inspecciona, convierte, renderiza el SVG resultante con
  Inkscape y comprueba que un flow complejo se rechaza sin cambiar el archivo.
- Evidencia de cierre: `npm run check` y `npm run test:mcp` pasan con Inkscape
  local. T18–T20 y T22–T24 ya contaban con sus pruebas MCP/unitarias; con T21
  cerrado queda completo el paquete de texto.

## F07-WP05 (cerrado 2026-08-29)

- `images_trace` ejecuta únicamente la action nativa verificada `object-trace`
  sobre una imagen local o embebida. El único preset es `default`, no expone
  parámetros de vectorización y comprueba el formato, los bytes y un máximo de
  4 megapíxeles antes de crear el bundle de Inkscape.
- La operación exige confirmación irreversible, revisión actual, una imagen
  identificada sin ambigüedad y devuelve el warning `IMAGE_TRACED_IRREVERSIBLY`.
  Las URLs y rutas no confinadas se rechazan mediante el resolver del workspace.
- El smoke MCP vectoriza un PNG local real y prueba el rechazo de un preset
  arbitrario; las pruebas unitarias cubren clasificación segura de fuentes
  embebidas o enlazadas.
- Evidencia de cierre: `npm run check` y `npm run test:mcp` pasan con Inkscape
  local. Con T25–T31 cerradas queda completo el paquete de imágenes y recursos.
- `gradients_manage` entrega CRUD tipado de gradientes linear/radial y stops,
  además de aplicación a fill o stroke. Las referencias `url(#id)` se validan
  al borrar; no se admite XML, CSS ni colores libres.
- `patterns_manage` entrega CRUD tipado de patrones dots/stripes, aplicables a
  fill o stroke, con unidades y matrices invertibles. Sus colores, tamaños y
  referencias se validan antes del commit; no acepta XML ni CSS libre.
- `markers_manage` entrega CRUD de markers de flecha/punto y su aplicación en
  inicio, medio o final de líneas y paths. El API limita explícitamente los
  destinos y no admite path data libre; pruebas unitarias y MCP lo cubren.
- El saneador conserva filtros SVG locales desconocidos y sus referencias, por
  lo que una operación de diseño no destruye efectos que todavía no son
  editables mediante el API; URLs de filtro externas permanecen rechazadas.
- `metadata_manage` añade title, description y licencia documental, junto con
  labels, nombres y ocultación ARIA por elemento. Sólo acepta texto plano
  acotado; RDF arbitrario y heurísticas de contraste siguen fuera del API.
- `text_path_manage` permite attach/detach de text-on-path sin convertir el
  texto: reubica los nodos de texto y tspan dentro del `textPath` y conserva
  el ID del texto. La conversión irreversible a paths sigue separada.
- `objects_to_paths` convierte rect/circle/ellipse/line/polygon/polyline/path
  seleccionados —o sus trazos— mediante acciones nativas en staging; exige
  revisión, confirmación explícita y publica backup, diff y warning
  irreversible. El smoke MCP real verifica `object-stroke-to-path`.
- `path_effects_inspect` inventaría efectos de trayecto locales y los paths que
  los referencian sin interpretar parámetros LPE. `path_effects_manage` permite
  desadjuntar paths explícitos o eliminar un efecto local ya sin referencias,
  siempre con revisión y backup; edición de parámetros y fixtures headless por
  tipo se mantienen fuera del catálogo.
- `connector_retarget` conserva la ruta de un conector Inkscape existente y
  reemplaza únicamente sus dos referencias locales de endpoints, con revisión
  y backup; routing y follow-after-move siguen pendientes.
- `connector_route` recalcula una polilínea ortogonal por centros de rect,
  circle o ellipse y respeta transformaciones axiales directas; puede evitar
  hasta 20 obstáculos explícitos de esas mismas formas con clearance tipado y
  conserva las referencias Inkscape. El seguimiento automático tras mover
  objetos no se anuncia.
- `palette_inspect` inventaría colores hex directos, estilos inline tipados,
  variables CSS locales y swatches sólidos nombrados de Inkscape;
  `palette_apply` actualiza un mapa explícito en atributos, estilos
  fill/stroke/stop-color, valores de variables y todos los stops directos,
  con revisión y backup. Colores indirectos complejos permanecen gateados.
- `color_management_inspect` informa perfiles SVG locales, referencias
  `icc-color()`, candidatas CMYK de cuatro componentes y nombres de perfil sin
  declaración local; no afirma conversión CMYK ni validación de output intent.
  PDF/X y preprensa externa permanecen explícitamente gateados.
- `flowed_text_inspect` localiza roots flowed y párrafos; `flowed_text_convert`
  convierte únicamente el caso simple de una región a texto SVG editable tras
  confirmación de pérdida. Flows complejos se rechazan para no simular layout.
- `path_node_move` mueve endpoints absolutos de líneas, curvas y arcos; y
  `path_node_edit` inserta/elimina nodos lineales, cambia L/T, abre/cierra un
  subpath y edita handles Q/C o parámetros de arco. `expand_smooth` convierte
  S/T a C/Q equivalentes para exponer sus handles reflejados. Los comandos
  relativos se normalizan a SVG absoluto equivalente; falta estabilidad de
  índices tras transformaciones complejas.
- `images_crop` aplica un `clipPath` local en unidades de usuario sobre una
  imagen SVG, sin rasterizar ni modificar su `href` o geometría. Requiere una
  revisión actual y rechaza reemplazar silenciosamente un clip existente; la
  cobertura unitaria y de cliente MCP verifica el flujo completo.
- `clips_manage` extiende el uso de clips a cualquier elemento: crea un
  rectángulo tipado, lo aplica o libera por IDs explícitos y rechaza borrar un
  recurso aún referenciado por SVG o CSS. `masks_manage` proporciona el mismo
  ciclo para una máscara rectangular opaca en espacio de usuario, sin permitir
  XML libre; ambas herramientas tienen cobertura unitaria y por cliente MCP.

## F07-WP06 (2026-08-27)

- `symbols_manage` lista, crea y elimina símbolos locales, y crea clones
  `<use>` con posición opcional. Verifica IDs, ausencia de referencias y la
  ausencia de ciclos antes de mutar; no abre una vía para XML arbitrario.
- `guides_grids_manage` ofrece inspección y CRUD tipado de guías y cuadrículas
  `xygrid` dentro de `sodipodi:namedview`. No toca preferencias globales de
  Inkscape y conserva revisiones/backup atómico en cada mutación.
- Evidencia: `npm run check` completó formato, lint, tipos y 177 pruebas
  unitarias (una omitida); `npm run test:mcp` completó las comprobaciones MCP
  modernas y legacy por stdio, incluidas ambas herramientas nuevas.

## F07-WP06 (completado el 2026-08-29)

- `metadata_manage` ahora materializa sólo un subconjunto RDF declarado y
  acotado: creator y keywords se serializan como `dc:creator` y
  `dc:subject`/`rdf:Bag`; title, description y licencia también quedan en
  `dc:*`. No recibe XML, RDF libre, URIs ni prefijos del cliente; conserva el
  valor legacy de licencia por compatibilidad.
- `path_effects_inspect` limita el inventario a 1.000 efectos y 1.000 usuarios
  por efecto, omite IDs no públicos y reduce tipos no seguros a `unknown` para
  no publicar atributos arbitrarios del documento. `path_effects_manage`
  mantiene su subconjunto headless de detach/delete, sin parámetros LPE.
- El cliente MCP inspecciona un LPE local y lo renderiza mediante Inkscape
  antes de comprobar detach/delete; las pruebas unitarias verifican RDF
  saneable, keywords únicos y el filtrado/bounding del inventario LPE.
- Evidencia de cierre: `npm run check` y `npm run test:mcp` pasan con
  Inkscape local. T32–T36 quedan cerradas; la edición paramétrica de LPE se
  mantiene explícitamente fuera del catálogo por no tener una semántica
  headless estable.

## F07-WP07 (completado el 2026-08-29)

- Los índices de `path_node_move` y `path_node_edit` quedan definidos sobre la
  secuencia de segmentos SVG canónica local. Un transform del elemento no
  renumera segmentos ni se aplana implícitamente; las mutaciones conservan el
  transform y continúan requiriendo la revisión actual.
- Las mutaciones geométricas y `elements_transform` reencaminan en la misma
  transacción los conectores semánticos compatibles unidos a endpoints
  movidos. El resultado expone `reroutedConnectorIds`; rutas con endpoint o
  transform no soportado se mantienen intactas y nunca se inventan.
- La paleta cubre `color`/`currentColor`, declaraciones CSS locales, variables,
  estilos inline, atributos y stops. Sólo reemplaza valores hex declarados en
  el mapa tipado; no intenta interpretar colores funcionales, perfiles ni CSS
  remoto.
- Evidencia de cierre: `npm run check` pasó con 259 pruebas unitarias y una
  omitida; `npm run test:mcp` pasó contra Inkscape local, incluyendo
  seguimiento de conectores y edición de nodo tras transformación. T37–T40
  quedan cerradas.
