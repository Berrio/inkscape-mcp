# F03 — Documentos, unidades, viewBox y páginas

## F03-WP01 (completo)

- ADR-010 fija 96 CSS px/in, separación de viewport físico y user units, y `preserve_user_scale` como política `page_only` por defecto.
- `geometry` modela longitudes físicas/CSS, páginas, rectángulos de usuario y matrices de resize explícitas.
- Los vectores normativos A4, contain y cover se prueban con tolerancias de punto flotante. El escalado DOM/CSS avanzado no se anuncia aún.
- `createSvgDocument` genera SVG con namespaces y `viewBox` coherente; `resizePageOnlySvg` actualiza página/viewBox sin transformar elementos.
- El catálogo MCP ya expone `workspace_list`, `workspace_list_documents` y `document_create` con schemas estrictos. `test:mcp` crea un A4 en un root temporal por stdio sin paths absolutos.
- `document_resize` implementa `page_only` con `expectedRevision`, backup, commit atómico y nueva revisión; la integración stdio confirma A4 → 148 × 210 mm sin modificar elementos.
- El primer pipeline `export_png` ya usa staging, runner seguro, opciones allowlisted, verificación PNG/IHDR y commit atómico. La integración local exportó 400 px con Inkscape MSIX 1.4.4; su contrato completo se ampliará en F05.
- `export_pdf` aplica el mismo pipeline y valida `%PDF-x.y`; la integración MCP real exportó PDF 1.5 con la instalación MSIX. El verificador ya lee page count y MediaBox; estrategia multipágina/subset y tolerancias siguen planificadas en F05.
- `export_svg` produce SVG Inkscape o plain SVG con verificación de XML/SVG seguro. El test MCP real cubre ahora PNG, PDF 1.5 y plain SVG desde el mismo A4.
- `document_inspect` devuelve viewport, `viewBox` y SHA-256 con control opcional de revisión, y se verifica después de un resize en el flujo MCP real.
- `document_preflight` detecta contenido activo, recursos externos, `foreignObject` y documentos inválidos sin mutarlos; se ejecuta antes de los exports en la integración MCP.
- `document_pages` implementa el adapter de páginas explícitas de Inkscape 1.4 (`sodipodi:namedview` + `inkscape:page`) y permite listar, añadir, actualizar, eliminar y reordenar IDs estables bajo revisión optimista. La integración MCP cubre alta y lectura antes de exportar.
- `document_settings` lee y actualiza `pagecolor`, `inkscape:pageopacity`, `inkscape:deskcolor`, `bordercolor` y `borderopacity` con valores por defecto observados en las plantillas de Inkscape 1.4.4; el flujo MCP verifica el round-trip tipado.
- `document_create` acepta dimensiones custom o presets versionados `a3`, `a4` y `letter` en retrato/paisaje; la prueba MCP crea A4 mediante `a4-portrait`.
- La creacion admite ahora una lista inicial de paginas explicitas de Inkscape; la integracion MCP crea `page_front`, agrega `page_back` y exporta el documento resultante.
- `document_inspect` incluye dimensiones, unidades de viewport, `viewBox`, paginas explicitas y revision SHA-256 en una respuesta de solo lectura.
- `document_resize` acepta los nueve anchors tipados para `page_only`; el plan geometrico conserva el centrado historico de contain/cover y calcula matrices de transformacion ancladas.
- `document_resize` aplica `scale_content_contain` y `scale_content_cover` mediante un grupo SVG transformado, preserva `defs`/metadata/namedview fuera del grupo y emite `CONTENT_MAY_BE_CROPPED` para cover. Un documento sin contenido renderizable recibe `NO_RENDERABLE_CONTENT` en vez de fallar.
- `scale_content_stretch` es un modo explicitamente solicitado; usa escala X/Y independiente y devuelve `NON_UNIFORM_CONTENT_SCALE` para que el cliente no confunda deformacion con contain/cover.
- `BoundsDescriptor` separa `geometric`, `visual` y `approximate`: el DOM
  actual nunca afirma cálculo geométrico exacto sin un motor SVG/CSS probado;
  `--query-all` se expone como observación visual de fidelidad `partial`.
  `elements_query(includeBounds=true)` devuelve ahora `kind`, `fidelity`,
  `source` y limitaciones junto a cada caja nativa.
- El corpus de fidelity cubre coordenadas negativas, transforms anidados,
  stroke, markers, filters, cascade/especificidad CSS, `!important`,
  `currentColor`, variables, porcentajes, `objectBoundingBox` y
  `non-scaling-stroke`; estas features se declaran approximate hasta disponer
  de un motor compatible validado.

### Mini-puerta F03-WP01

- Objetivo verificable: mantener separadas página física, viewBox, user units y
  contenido, y no presentar bounds nativos o DOM como exactos fuera de un
  motor probado.
- Prerrequisitos/puertas: ADR-010, modelo `geometry` y bundles nativos de
  F02; no se introdujo un selector CSS ni un motor de layout nuevo.
- Interfaces: `BoundsDescriptor`, `assessSvgBounds`,
  `nativeVisualBoundsDescriptor` y `elements_query` existente.
- No-objetivos: cálculo numérico de bounds geométricos/CSS computado o
  expansión visual de stroke/filter; se registran como limitaciones.
- Evidencia: `npm test -- --run tests/unit/bounds.test.ts`
  `tests/unit/geometry.test.ts` `tests/unit/documents.test.ts` (21/21),
  `npm run check` (17 archivos, 84 pruebas correctas y 1 omitida) y
  `npm run test:mcp` (stdio moderno y legacy) el 2026-08-26.
- T00–T06 están cerradas. El siguiente WP lógico es `F03-WP02`, para SVG
  mínimo, presets e inspección de dimensiones ambiguas; no se adelanta esa
  normalización en este paquete.

## F03-WP02 (completo)

- Objetivo verificable: crear/inspeccionar documentos SVG mínimos con medidas
  inequívocas y, ante dimensiones ausentes, unitless o porcentuales, exponer
  la normalización y sus límites sin alterar el archivo durante la lectura.
- Prerrequisitos/puertas: F03-WP01 completo. `document_create`, presets,
  páginas e inspección base ya existían; este paquete completa T11.
- Interfaces: `inspectSvgSettings`, `parseViewportLength`,
  `document_inspect`, `document_resize` y `document_preflight` existentes.
- No-objetivos: resolver porcentajes contra una ventana/DOM host, alterar un
  documento durante `document_inspect`, o aplicar una normalización silenciosa
  antes de exportar.
- Resultado: dimensiones faltantes usan el viewport SVG 300 × 150 px con
  warnings estables; longitudes sin unidad se hacen CSS px explícitos; un
  `viewBox` ausente se deriva del viewport concreto a 96 dpi. Porcentajes se
  muestran con fallback marcado `percentage_fallback`, `ambiguousViewport` y
  warnings por eje; resize los rechaza hasta que se normalicen explícitamente.
  Preflight propaga estos warnings como recomendaciones sin falsificar error.

### Mini-puerta F03-WP02

- T07–T10 estaban cubiertas por el SVG mínimo, presets, creación e inspección
  existentes; T11 cubre missing/unitless/percentage/viewBox y el rechazo de
  mutación sobre porcentajes ambiguos.
- Evidencia: `npm test -- --run tests/unit/documents.test.ts`
  `tests/unit/geometry.test.ts` (19/19), `npm run check` (17 archivos, 86
  pruebas correctas y 1 omitida) y `npm run test:mcp` (stdio moderno y legacy,
  incluyendo porcentaje ambiguo) el 2026-08-26.
- Archivos: `src/documents/basic.ts`, `src/documents/preflight.ts`,
  `src/documents/index.ts`, `src/server/index.ts`,
  `tests/unit/documents.test.ts`, `scripts/test-mcp.mjs`, el plan y este
  informe.
- El siguiente WP lógico es `F03-WP03` (resize de contenido, fit y
  crop/orientación); no se adelanta ese trabajo aquí.

## F03-WP03 (completo)

- Objetivo verificable: ajustar página sin transformar objetos mediante fit a
  drawing/selección con márgenes por lado, crop/expand físicos y orientación.
- Prerrequisitos/puertas: F03-WP01/02 completos; los bounds nativos se
  mantienen declarados como `visual`/`partial` y no se confunden con geometría
  DOM exacta.
- Interfaces: `document_fit_page`, `document_page_adjust`,
  `fitPageToBoundsSvg`, `adjustPageMarginsSvg` y
  `changePageOrientationSvg`.
- No-objetivos: fit a paths sin ID, calcular CSS/stroke/filter geométricos o
  mover/escalar objetos para hacerlos caber.
- Resultado: fit usa `--query-all` sobre el bundle nativo inmutable; admite
  drawing completo o selección de IDs y devuelve bounds/fidelidad parcial más
  `FIT_USED_VISUAL_BOUNDS`. Crop y expand cambian tanto viewport como viewBox
  por cada margen físico, rechazan área no positiva y preservan el markup de
  objetos. El toggle de orientación intercambia dimensiones bajo un anchor y
  emite `PAGE_ORIENTATION_CHANGED`.

### Mini-puerta F03-WP03

- T12–T15 estaban verificadas por los modos page-only/contain/cover/stretch;
  T16 cubre fit visual y T17 crop, expand y orientación.
- Evidencia: `npm test -- --run tests/unit/documents.test.ts`
  `tests/unit/geometry.test.ts` (21/21), `npm run check` (17 archivos, 88
  pruebas correctas y 1 omitida) y `npm run test:mcp` (stdio moderno y legacy,
  fit por selección con Inkscape real, crop y orientación) el 2026-08-26.
- Archivos: `src/documents/basic.ts`, `src/documents/index.ts`,
  `src/server/index.ts`, `tests/unit/documents.test.ts`,
  `scripts/test-mcp.mjs`, el plan y este informe.
- El siguiente WP lógico es `F03-WP04` (dry run y validación de páginas); no
  se adelanta ese trabajo aquí.

## F03-WP04 (completo)

- Objetivo verificable: prever un resize con tamaño, transformación y diff sin
  escribir, y comprobar consistencia del layout de páginas explícitas.
- Prerrequisitos/puertas: F03-WP01–03 completos; diff semántico de F02 y
  bounds visuales nativos de Inkscape están disponibles.
- Interfaces: `document_resize(dryRun=true)`, `document_page_validate`,
  `summarizeSvgDiff` y `validateSvgPageLayout`.
- No-objetivos: prometer que bounds de objetos son geométricos exactos,
  normalizar objetos sin ID ni convertir el diagnóstico de una página vacía en
  una mutación automática.
- Resultado: dry run comprueba la revisión, calcula SVG en memoria y retorna
  página/viewBox previstos, matriz de contenido cuando aplica, warnings y diff
  resumido; no crea backup ni modifica la revisión. La validación enumera
  solapamientos exactos entre rectángulos de página y, con bounds nativos
  marcados `partial`, páginas sin objeto visual y objetos fuera de toda página.

### Mini-puerta F03-WP04

- T18/T20/T22/T23 estaban verificadas por el adapter/pages/settings. T19
  cubre predicción de resize sin escritura; T21 cubre solapamiento, vacío y
  fuera de página con fidelidad explícita.
- Evidencia: `npm test -- --run tests/unit/documents.test.ts`
  `tests/unit/svg-diff.test.ts` (21/21), `npm run check` (17 archivos, 89
  pruebas correctas y 1 omitida) y `npm run test:mcp` (stdio moderno y legacy,
  dryRun y page validation con Inkscape real) el 2026-08-26.
- Archivos: `src/documents/basic.ts`, `src/documents/pages.ts`,
  `src/documents/index.ts`, `src/server/index.ts`,
  `tests/unit/documents.test.ts`, `scripts/test-mcp.mjs`, el plan y este
  informe.
- F03 ya tiene todos sus WP implementados; sus puertas F03-G01–G09 se auditan
  en la fase de gates/fixtures y no se marcan con evidencia parcial.

## F03-G01 (cerrada)

- Objetivo: verificar el tamaño físico A4 después de un round-trip nativo, no
  sólo en las conversiones DOM.
- Evidencia: el smoke MCP por stdio crea `a4.svg` con `a4-portrait`, lo exporta
  con `export_svg(flavor=inkscape)` y reabre `a4-round-trip.svg` con
  `document_inspect`. Exige `mm` como unidad de ambos ejes y 210 × 297 mm con
  tolerancia absoluta de **0,01 mm** por eje.
- Resultado: `npm run test:mcp` pasó el 2026-08-30 con las negociaciones MCP
  fijada `2026-07-28` y legacy; la comprobación recorre el runner nativo y no
  acepta una conservación meramente declarativa del SVG inicial.
- Archivos: `scripts/test-mcp.mjs`, `PLAN_IMPLEMENTACION.md` y este informe.
- Riesgo residual: la evidencia se fija para Inkscape 1.4.4 en Windows; otras
  versiones/plataformas siguen sujetas a sus capability gates.

## F03-G02 (cerrada)

- Objetivo: demostrar que `document_resize(mode=page_only)` no modifica la
  geometría ni los transforms de los elementos del documento.
- Evidencia: el vector unitario compara todos los atributos de los hijos SVG
  antes/después de redimensionar A4 a 148 × 210 mm. El smoke MCP repite el
  contrato por stdio con un grupo transformado anidado, un `rect` con
  geometría/rotación y un `path` con `d` y matriz; sólo se permite cambiar el
  viewport y el `viewBox` del root.
- Resultado: `npm test -- --run tests/unit/documents.test.ts` pasó (35/35) y
  `npm run test:mcp` pasó el 2026-08-30 con negociación MCP fijada y legacy.
- Archivos: `tests/unit/documents.test.ts`, `scripts/test-mcp.mjs`,
  `PLAN_IMPLEMENTACION.md` y este informe.
- Riesgo residual: la garantía es semántica de atributos SVG; un cliente que
  interprete estilos CSS complejos sigue cubierto por sus gates de fidelidad,
  no por este resize sin transformación.

## F03-G03 (cerrada)

- Objetivo: probar que contain, cover y fit calculan escala, bounds y anchor
  esperados en fixtures mecánicos.
- Evidencia: el modelo puro fija 800 × 600 → 1080 × 1080: contain usa escala
  1,35 y cover 1,8; `top_left` deja offset cero y `bottom_right` produce
  respectivamente `(0,270)` y `(-360,0)`. El vector de fit con bounds
  `(-10,20,100,50)` y 3 mm por lado produce `(-13,17,106,56)`.
- Smoke MCP: por stdio comprueba las matrices de contain/cover y el warning
  `CONTENT_MAY_BE_CROPPED`; además, el fit de un rectángulo nativo confirma
  bounds `(10,5,30,20)`, página 40 × 25 mm y viewBox `(5,3,40,25)`.
- Tolerancias: 12 decimales en cálculo DOM puro; 0,01 unidades en los valores
  observados por Inkscape, que se anuncian como visuales de fidelidad parcial.
- Resultado: `npm test -- --run tests/unit/geometry.test.ts`
  `tests/unit/documents.test.ts` pasó (40/40) y `npm run test:mcp` pasó el
  2026-08-30 con negociación MCP fijada y legacy.
- Archivos: `tests/unit/geometry.test.ts`, `tests/unit/documents.test.ts`,
  `scripts/test-mcp.mjs`, `PLAN_IMPLEMENTACION.md` y este informe.
- Riesgo residual: los bounds de `document_fit_page` proceden de Inkscape y
  permanecen `partial`; la exactitud CSS/pintura compleja se controla en G08.

## F03-G04 (cerrada)

- Objetivo: confirmar que las páginas explícitas Inkscape 1.4 mantienen sus
  IDs y referencias documentales durante todo el CRUD y un reorder.
- Evidencia: el vector unitario parte de `page_first`, añade `page_second`, lo
  actualiza, reordena a `page_second,page_first` y borra el primero. El único
  resultado conserva `page_second`, su label y rectángulo; también conserva la
  guía y `inkscape:current-page="page_second"` del namedview.
- Smoke MCP: ejecuta la misma secuencia por `document_pages` bajo revisión
  optimista, incluida lectura final con la revisión producida por el delete.
  Verifica orden tras add/reorder, label/posición tras update y la referencia
  del namedview en el SVG publicado.
- Resultado: `npm test -- --run tests/unit/documents.test.ts` pasó (37/37) y
  `npm run test:mcp` pasó el 2026-08-30 con negociación MCP fijada y legacy.
- Archivos: `tests/unit/documents.test.ts`, `scripts/test-mcp.mjs`,
  `PLAN_IMPLEMENTACION.md` y este informe.
- Riesgo residual: el adapter está deliberadamente fijado al modelo
  `inkscape:page` de Inkscape 1.4; la migración `svg:view` de 1.5 sigue
  gateada por versión/plataforma.

## F03-G05 (cerrada)

- Objetivo: hacer visible y verificable la frontera de mutación atómica de
  tamaño/página: revisión vigente, backup, nueva revisión y diff sin contenido.
- Cambio: `document_resize` devuelve ahora `diff` también cuando publica la
  mutación; antes sólo lo exponía dentro de `predicted` para dry-run. El mismo
  resumen semántico acotado se usa en ambos caminos, sin incluir SVG ni paths.
- Smoke MCP: el resize in-place exige `backupCreated`, una revisión distinta y
  un diff con conteos coherentes. Un segundo resize con la revisión anterior
  devuelve error y el SHA-256 del documento sigue siendo el de la publicación
  válida. El dry-run comprueba que su `diff` superior es idéntico al predicho.
- Resultado: pruebas focalizadas de schema/diff pasaron (3/3) y
  `npm run test:mcp` pasó el 2026-08-30 con negociación MCP fijada y legacy.
- Archivos: `src/server/index.ts`, `scripts/test-mcp.mjs`,
  `docs/tool-reference.md`, `docs/design-size-guide.md`,
  `PLAN_IMPLEMENTACION.md` y este informe.
- Riesgo residual: el diff resume IDs y conteos; cambios del root como tamaño o
  viewBox no inventan un ID cambiado y se leen mediante el tamaño previsto o
  inspección posterior.

## F03-G06 (cerrada)

- Objetivo: dejar una evidencia visual legible y reproducible de los cuatro
  resultados de resize que no depende de una captura manual ni de una ventana
  activa.
- Evidencia: `npm run evidence:f03` compila el servidor, crea un workspace
  temporal y usa el protocolo MCP por stdio con Inkscape headless para copiar
  la muestra vectorial de primera parte y producir previews de 240 px de ancho.
  Cada mutacion exige su revision; `cover` exige ademas
  `CONTENT_MAY_BE_CROPPED`. El script publica solamente los PNG y el manifiesto
  de evidencia dentro de este repositorio y limpia el workspace temporal.
- Comparativo (origen 800 x 600 px, destino 600 x 600 px):

  | Original                                              | `page_only`                                                                             |
  | ----------------------------------------------------- | --------------------------------------------------------------------------------------- |
  | ![Original 800 por 600](assets/f03-resize-source.png) | ![Page only 600 por 600](assets/f03-resize-page-only.png)                               |
  | Mantiene la pagina horizontal.                        | Cambia viewport/viewBox sin transformar el contenido; por eso se ve el recorte derecho. |

  | `scale_content_contain`                                                       | `scale_content_cover`                                                  |
  | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
  | ![Contain 600 por 600](assets/f03-resize-contain.png)                         | ![Cover 600 por 600](assets/f03-resize-cover.png)                      |
  | Escala proporcionalmente y conserva todo el dibujo, dejando espacio vertical. | Llena el cuadro conservando proporcion y puede recortar los laterales. |

- Artefactos: la muestra esta en
  `docs/progress/assets/f03-resize-comparison.svg`; los cuatro PNG y
  `f03-resize-previews.json` son resultado reproducible. El JSON conserva
  renderer, dimensiones, modo y SHA-256 de cada artefacto, de modo que una
  regeneracion se puede auditar sin incluir el documento o rutas temporales en
  la respuesta MCP.
- Resultado: `npm run evidence:f03` paso el 2026-08-30 con Inkscape 1.4.4
  headless en Windows. La inspeccion visual de los PNG confirma crop en
  `page_only`/`cover` y preservacion completa en `contain`.
- Archivos: `scripts/generate-f03-preview-evidence.mjs`,
  `docs/progress/assets/f03-resize-comparison.svg`, los PNG/manifest generados,
  `package.json`, el plan y este informe.
- Riesgo residual: estos previews fijan evidencia para Inkscape 1.4.4 en
  Windows y no sustituyen las tolerancias numericas y de CSS que cierran G07 y
  G08.

## F03-G07 (cerrada)

- Objetivo: hacer que los cinco vectores normativos de la seccion 10.2 sean
  contratos localizables y que su tolerancia no quede implicita en cada prueba.
- Decisión: ADR-010 fija para el modelo geometrico puro
  `toBeCloseTo(..., 12)`, equivalente a una diferencia absoluta menor que
  `5e-13` en la unidad de cada vector. Warnings se comparan de forma exacta.
  La tolerancia nativa de 0,01 unidades de F03-G03 no cambia: sirve para
  observaciones visuales parciales de Inkscape, no para esconder drift del
  modelo puro.
- Evidencia: `tests/unit/geometry.test.ts` identifica F03-G07 y comprueba A4
  con `preserve_user_scale`, A4 con `preserve_viewbox` y factor fisico 2,
  contain centrado y cover centrado con sus matrices y warnings. La quinta
  comprobacion, en `tests/unit/documents.test.ts`, ajusta bounds
  `(-10,20,100,50)` con margen de 3 mm y exige el rectangulo
  `(-13,17,106,56)` mas `FIT_USED_VISUAL_BOUNDS`.
- Resultado: `npm test -- --run tests/unit/geometry.test.ts
tests/unit/documents.test.ts` paso con 42/42 pruebas el 2026-08-30.
- Archivos: `docs/architecture/adr-010-document-geometry.md`,
  `tests/unit/geometry.test.ts`, `tests/unit/documents.test.ts`, el plan y este
  informe.
- Riesgo residual: G07 prueba el modelo y el adaptador determinista; el
  subconjunto CSS/fidelidad del escalado de contenido permanece deliberadamente
  pendiente en G08.
