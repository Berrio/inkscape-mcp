# F05 - Exportaciones

## F05-WP01 (completado el 2026-08-26)

- `exportSpecSchema` implementa el contrato discriminado por formato para PNG,
  PDF, SVG/plain SVG/SVGZ y formatos adicionales gateados. Rechaza propiedades
  cruzadas, áreas custom fuera de PNG, selección/páginas múltiples sin destino
  de directorio, templates inseguros, rutas absolutas/traversal y overwrite sin
  la revisión de salida esperada.
- `ExportAreaNormalizer` se amplió para page, drawing, selección única y área
  custom. La planificación de múltiples páginas o selección `each` queda
  explícita para el planificador de variantes de F05-WP05: nunca se convierte
  en un argv ambiguo.
- Los builders puros generan únicamente flags allowlisted desde el spec ya
  validado. El pipeline común emite etapas ordenadas
  `validated→staging→rendering→verifying→publishing→completed`, propaga
  cancelación y garantiza cleanup del staging del adaptador.
- El manifiesto común registra request normalizado, versión Inkscape, hashes de
  artefactos, estrategia y warnings sin rutas absolutas. `ArtifactStore` admite
  metadata de formato/verificación y publicación de lote opaca con rollback
  de copias si falla un miembro; no se describe como atómica ante crash.
- El dispatcher de verificación solo acepta PNG/PDF/SVG/plain-SVG cuando puede
  probar su estructura. Los demás formatos devuelven falta de verifier hasta
  tener capability, fixture y adaptador real.

### Mini-gate F05-WP01

- `npm run check` pasó: formato, lint, tipos y 105 pruebas (una omitida).
- `npm run test:mcp` pasó con Inkscape local y los transportes moderno/legado.
- Pruebas unitarias cubren la unión discriminada, combinaciones cruzadas,
  rutas/overwrite inseguros, múltiples variantes, builders, progreso/cancelación,
  cleanup, manifiesto y rollback/metadata de artifacts.

## F05-WP03 (inicio)

- `export_pdf` usa `pdf-lib` 1.17.1 (MIT) para rechazar PDFs no parseables y
  devolver version, numero de paginas y MediaBox por pagina; la prueba crea un
  PDF de dos paginas con boxes conocidos y el flujo MCP verifica un PDF real
  de Inkscape.
- Aun falta la sonda multipagina especifica de Inkscape 1.4.4, CropBox,
  tolerancias, subset, LaTeX y contratos completos de opciones PDF.

## F05-WP02 (en curso)

- `export_png` acepta DPI explicito de 0 a 9600 y lo envia mediante el flag
  allowlisted de Inkscape. Rechaza mezclar DPI con width/height para no ocultar
  la precedencia del motor.
- El verificador PNG inspecciona IHDR y, cuando existe, `pHYs`, devolviendo DPI
  X/Y observado. La integracion MCP exporta un PNG a 144 DPI desde Inkscape.
- PNG admite `background=document|transparent|solid`. `document` usa
  `pagecolor`/`pageopacity` y nunca el desk; `solid` exige color hexadecimal y
  puede definir opacidad. La integracion MCP valida una exportacion roja.
- PNG declara `area=page|drawing` y pasa solo `--export-area-page` o
  `--export-area-drawing`; la integracion crea una forma visible y valida la
  ruta drawing con Inkscape real.
- `export_svg` acepta `textToPath` de forma explicita y devuelve
  `TEXT_CONVERTED_TO_PATHS`; el test MCP cubre el flag para que la conversion
  no quede silenciosa.
